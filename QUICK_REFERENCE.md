# 快速参考卡片

## 🚀 5分钟快速指南

### 安装和加载

```bash
# 1. 安装依赖
npm install

# 2. 构建项目
npm run build

# 3. 在 Chrome 中加载
# chrome://extensions/
# → 启用开发者模式
# → 加载已解压的扩展程序
# → 选择 dist 文件夹
```

### 生成 CRX 文件

```bash
# 方法1: 使用 Chrome 浏览器（推荐）
# chrome://extensions/
# → 找到扩展
# → 点击"打包扩展程序"
# → 选择 dist 文件夹

# 方法2: 使用脚本（需先 npm install --save-dev crx3）
npm run build:crx
```

---

## 📂 文件结构速查

```
dist/                       # 构建输出（加载此文件夹）
├── manifest.json
├── popup.html
├── popup.js
├── service-worker.js
├── content-script.js
└── icons/

src/                        # 源代码
├── popup.tsx              # 主UI组件
├── types/index.ts         # 类型定义
└── main.tsx               # 入口

public/                     # 静态资源
├── manifest.json
├── popup.html
├── service-worker.js
└── content-script.js
```

---

## 📚 文档速查表

| 需求 | 查看文件 | 时间 |
|------|---------|------|
| 快速开始 | [QUICK_START.md](QUICK_START.md) | 5分钟 |
| 完整说明 | [README.md](README.md) | 20分钟 |
| 安装指南 | [INSTALLATION.md](INSTALLATION.md) | 10分钟 |
| 开发指南 | [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) | 30分钟 |
| API 参考 | [docs/API_REFERENCE.md](docs/API_REFERENCE.md) | 25分钟 |
| 使用案例 | [docs/EXAMPLES.md](docs/EXAMPLES.md) | 20分钟 |
| CRX 打包 | [PACKAGING_CRX.md](PACKAGING_CRX.md) | 15分钟 |
| 项目总结 | [FINAL_SUMMARY.md](FINAL_SUMMARY.md) | 10分钟 |

---

## 🔑 关键命令

```bash
npm install           # 安装依赖
npm run dev           # 开发服务器（http://localhost:5173）
npm run build         # 构建项目
npm run lint          # 代码检查
npm run build:crx     # 生成CRX文件

# 或直接
npm run build && npm run copy-assets  # 完整构建流程
```

---

## 🎯 城市代码快速查询

```
北京      101010100    深圳      101210100
上海      101020100    杭州      101280100
南京      101190400    成都      101260100
西安      111110100    武汉      101200100
天津      101030100    长沙      101220100
```

---

## 📋 功能快速验证清单

### 建立后检查
- [ ] `npm run build` 执行成功
- [ ] `dist/` 文件夹已生成
- [ ] 必需文件都存在

### Chrome 中加载后检查
- [ ] 插件图标出现在右上角
- [ ] 点击图标弹出UI窗口
- [ ] 可以配置筛选条件
- [ ] 可以点击刷新按钮
- [ ] 浏览器刷新后数据仍存在

### 功能测试
- [ ] 能配置岗位名称
- [ ] 能选择城市
- [ ] 能调整薪资范围
- [ ] 能添加/删除技能标签
- [ ] 能采集数据
- [ ] 能查看结果

---

## 🐛 常见问题速解

| 问题 | 解决方案 |
|------|---------|
| 插件无法加载 | 检查 `dist` 文件夹是否存在 |
| 采集失败 | 检查网络连接，更换筛选条件重试 |
| 数据丢失 | 检查浏览器是否禁用了 Storage API |
| 样式错乱 | 清除浏览器缓存，重新加载扩展 |
| TypeScript 错误 | 运行 `npm install` 重新安装依赖 |

---

## 🌐 招聘网站代码

```
BOSS直聘      boss
猎聘          liepin
51Job         51job
智联招聘      zhaopin
脉脉高聘      maimengaoping
```

---

## 💾 数据存储结构

```typescript
// Chrome Storage 中保存的数据
{
  filterConfig: {           // 筛选配置
    website: 'boss',
    city: '101010100',
    district: '',
    jobTitle: '',
    jobType: '',
    minAge: 25,
    maxAge: 35,
    minSalary: 15,
    maxSalary: 25,
    skills: ['React']
  },
  jobsData: [],            // 采集的职位列表
  stats: {                 // 统计信息
    total: 0,
    lastUpdate: '',
    source: 'BOSS直聘'
  }
}
```

---

## 🔄 开发工作流

```
编辑代码
    ↓
保存文件
    ↓
npm run build
    ↓
Chrome 扩展页面点击"重新加载"
    ↓
刷新插件窗口测试
    ↓
打开 DevTools 调试
    ↓
查看 console 输出
```

---

## 📦 CRX 文件生成步骤

### 步骤1: 确保构建成功
```bash
npm run build
```

### 步骤2: 打包扩展
```
chrome://extensions/
  ↓
找到"招聘数据采集器"扩展
  ↓
点击"打包扩展程序"按钮
  ↓
选择 dist 文件夹
  ↓
点击"打包扩展程序"
```

### 步骤3: 获取文件
```
recruitment-data-collection-.crx      # CRX文件
recruitment-data-collection-.pem      # 私钥（妥善保管！）
```

---

## 🔐 私钥管理速览

```bash
# 保存私钥
mkdir -p ~/.chrome-extensions
cp recruitment-data-collection-.pem ~/.chrome-extensions/
chmod 600 ~/.chrome-extensions/recruitment-data-collection-.pem

# 使用现有私钥重新签名
# 在 Chrome 中：chrome://extensions/ → 打包扩展程序
# 在"私钥文件"字段中选择之前保存的 .pem 文件
```

---

## 📊 项目统计速记

```
💾 代码     : 2000+ 行
📚 文档     : 15000+ 字
📁 文件数   : 15+
🎯 功能     : 10+
⏱️ 开发时间 : 1-2 周
✅ 完成度   : 100%
```

---

## 🎓 类型定义速查

```typescript
// 筛选配置
FilterConfig {
  website: string,      // 招聘网站
  city: string,         // 城市代码
  jobTitle: string,     // 职位名称
  minAge: number,       // 最小年龄
  maxAge: number,       // 最大年龄
  minSalary: number,    // 最小薪资
  maxSalary: number,    // 最大薪资
  skills: string[]      // 技能标签
}

// 职位数据
JobData {
  id: string,           // 职位ID
  name: string,         // 职位名称
  company: string,      // 公司名称
  city: string,         // 工作城市
  salary: string,       // 薪资范围
  skills: string[],     // 所需技能
  matchScore: number    // 匹配度评分
}
```

---

## 🚀 版本更新步骤

```bash
# 1. 修改代码
# src/popup.tsx, public/service-worker.js, etc.

# 2. 更新版本号
# manifest.json: "version": "1.0.1"
# package.json:  "version": "1.0.1"

# 3. 构建和打包
npm run build
# 使用相同的 .pem 私钥打包新的 CRX

# 4. 版本控制
git add .
git commit -m "Release v1.0.1"
git tag -a v1.0.1 -m "Release version 1.0.1"
```

---

## 📞 文档导航树

```
FINAL_SUMMARY.md (项目完成总结)
    ├── README.md (详细说明)
    ├── QUICK_START.md (快速开始)
    ├── INSTALLATION.md (安装指南)
    ├── PACKAGING_CRX.md (CRX打包)
    └── docs/
        ├── README.md (文档导航)
        ├── DEVELOPMENT.md (开发指南)
        ├── API_REFERENCE.md (API参考)
        └── EXAMPLES.md (使用案例)
```

---

## ✨ 最后检查清单

发布前确保：

- [ ] 版本号已更新
- [ ] 代码已通过 ESLint 检查
- [ ] 没有 TypeScript 错误
- [ ] 构建成功输出
- [ ] 所有文件都在 dist 中
- [ ] 功能已测试
- [ ] 文档已更新
- [ ] 私钥已备份

---

## 🎯 快速命令速记

```bash
# 新手必用
npm install              # 首次安装
npm run build            # 构建项目
# 然后在 Chrome 加载 dist 文件夹

# 开发必用
npm run dev              # 开发服务器
npm run lint             # 检查代码

# 发布必用
npm run build:crx        # 生成 CRX（需 crx3）
```

---

**💡 提示**: 保存此文件到书签，快速查询常用信息！

**最后更新**: 2025年12月11日

